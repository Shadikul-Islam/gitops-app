apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/infrastructure
  prune: true
  dependsOn:
    - name: sources
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
